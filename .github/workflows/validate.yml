name: Validate and package

on:
  push:
    branches:
      - '**'
  pull_request:
  workflow_dispatch:
  workflow_call:
    inputs:
      release_tag:
        description: Optional release tag that must match the plugin version
        required: false
        type: string
      checkout_ref:
        description: Optional git ref to validate and package
        required: false
        type: string

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG_INPUT: ${{ inputs.release_tag }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.checkout_ref || github.ref }}

      - name: Validate source, version, tests, and archive
        shell: bash
        run: |
          set -euo pipefail

          while IFS= read -r php_file; do
            php -l "$php_file" >/dev/null
          done < <(find Upload tests -type f -name '*.php' | sort)

          while IFS= read -r js_file; do
            node --check "$js_file" >/dev/null
          done < <(find Upload -type f -name '*.js' | sort)

          php tests/spam_preventer_test.php

          version="$(sed -n "s/.*'version'[[:space:]]*=>[[:space:]]*'\([^']*\)'.*/\1/p" Upload/inc/plugins/spam_preventer.php | head -n 1)"
          if [ -z "$version" ]; then
            echo "Plugin version is missing." >&2
            exit 1
          fi
          if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Plugin version is invalid: $version" >&2
            exit 1
          fi
          if [[ -n "$RELEASE_TAG_INPUT" && "${RELEASE_TAG_INPUT#v}" != "$version" ]]; then
            echo "Tag $RELEASE_TAG_INPUT does not match plugin version $version." >&2
            exit 1
          fi
          if ! grep -E "^## \\[?$version\\]?([ -]|$)" CHANGELOG.md >/dev/null; then
            echo "CHANGELOG.md has no release heading for $version." >&2
            exit 1
          fi

          mkdir -p dist
          archive="dist/mybb-spam-preventer-$version.zip"
          zip -X -q -r "$archive" Upload tests README.md LICENSE CHANGELOG.md default-blocked-phrases.txt

          archive_contents="$(unzip -Z1 "$archive")"
          required_entries=(
            Upload/inc/plugins/spam_preventer.php
            Upload/inc/languages/english/spam_preventer.lang.php
            Upload/jscripts/spam-preventer/quick-reply-errors.js
            tests/spam_preventer_test.php
            README.md
            LICENSE
            CHANGELOG.md
            default-blocked-phrases.txt
          )
          for required_entry in "${required_entries[@]}"; do
            grep -F -x "$required_entry" <<< "$archive_contents" >/dev/null || {
              echo "Archive is missing required entry: $required_entry" >&2
              exit 1
            }
          done

          echo "Validation and packaging passed for Spam Preventer $version."

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: mybb-spam-preventer
          path: dist/*.zip
          if-no-files-found: error
